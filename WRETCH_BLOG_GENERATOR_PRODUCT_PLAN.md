# 無名小站網誌產生器－產品計畫書

## 1. 文件資訊

- 產品名稱：無名小站網誌產生器
- 文件版本：v0.1 Draft
- 文件日期：2026-09-04
- 產品類型：瀏覽器端靜態網站產生器
- 目標平台：GitHub Pages、一般靜態網站空間、本機瀏覽器

## 2. 產品概要

「無名小站網誌產生器」讓持有無名小站備份的使用者，在瀏覽器中選取自己的 Blog 備份檔，產生可閱讀、可預覽及可匯出的靜態網誌。

所有備份解析與網站產生工作預設在使用者的瀏覽器內完成。系統不將備份檔、文章或密碼上傳到伺服器，也不自動公開產生成品。

## 3. 產品目標

1. 降低一般使用者閱讀無名小站備份的技術門檻。
2. 保留文章標題、正文、日期、公開狀態、標籤及歷史留言。
3. 提供接近舊式無名小站的視覺化閱讀介面。
4. 讓使用者先在瀏覽器預覽，再自行決定是否下載或公開。
5. 避免備份資料在產生過程中離開使用者裝置。

## 4. 隱私與安全定位

### 4.1 本機處理原則

- 使用者透過瀏覽器檔案選擇器載入備份。
- JavaScript 使用 File API 直接讀取本機檔案。
- 備份內容不傳送到網站伺服器、第三方 API、分析服務或雲端儲存空間。
- 產生結果預設只保留在目前瀏覽器工作階段。
- 重新整理或關閉頁面後，使用者需重新選擇備份檔。
- 產品不得自動發布、上傳或產生公開網址。

在上述條件成立時，沒有取得使用者裝置或備份檔的人，無法從產生器網站讀取網誌內容。

### 4.2 閱讀確認密碼

上鎖文章的密碼功能定義為「閱讀確認遮罩」，不屬於加密、登入或權限控制：

- `isCloak=0` 的文章直接顯示。
- `isCloak=1` 與 `isCloak=2` 的文章先顯示標題與密碼欄位。
- 密碼正確後在頁面上展開正文與公開留言。
- 產出的靜態 HTML 仍包含原始正文。
- 使用者一旦將成品公開上網，能檢視網站檔案的人仍可能讀取上鎖文章。

產品介面必須清楚提示：「此密碼僅為閱讀確認，不提供資料加密保護。」

### 4.3 公開責任

- 預覽行為不等於公開。
- 下載產生結果不等於公開。
- 只有使用者自行將成品部署至 GitHub Pages 或其他網路空間後，才會形成可供他人存取的網址。
- 產品不應宣稱公開後的上鎖文章具有真正的保密性。

## 5. 目標使用者

- 持有無名小站官方匯出備份的原網誌作者。
- 希望在本機重新閱讀舊文章的使用者。
- 希望將公開文章重新整理成靜態網站的使用者。
- 不熟悉 Python、終端機或網站建置流程的一般使用者。

## 6. MVP 使用流程

```text
進入產生器首頁
      │
      ▼
選擇 Blog XML 與 Movable Type 檔案
      │
      ▼
系統在瀏覽器內檢查及解析檔案
      │
      ▼
顯示文章、留言與上鎖文章統計
      │
      ▼
設定上鎖文章的閱讀確認密碼
      │
      ▼
按下「產生網誌」
      │
      ▼
進入預覽畫面
      │
      ├── 返回修改設定
      └── 下載靜態網站 ZIP
```

## 7. 頁面需求

### 7.1 產生器首頁

檔案：`index.html`

頁面標題：

```text
無名小站網誌產生器
```

首頁包含：

1. 產品名稱與簡短說明。
2. 隱私提示：「檔案只在此瀏覽器處理，不會上傳。」
3. Blog XML 檔案選擇欄位。
4. Movable Type 檔案選擇欄位。
5. 上鎖文章閱讀確認密碼輸入框。
6. 「產生網誌」主要按鈕。
7. 檔案檢查結果與錯誤提示區域。

檔案欄位建議標示：

```text
選擇 Blog XML
選擇 Movable Type 備份
```

密碼欄位：

- 標籤：「上鎖文章閱讀密碼」
- placeholder：「設定閱讀密碼」
- 類型：`password`
- 使用者的備份包含 `isCloak=1/2` 文章時必填。
- 欄位下方顯示閱讀確認遮罩的限制說明。

主要按鈕：

```text
產生網誌
```

按鈕在必要檔案尚未通過檢查前保持停用。

### 7.2 資料確認區

兩個檔案解析成功後，首頁顯示：

- Blog 名稱。
- 作者暱稱。
- 文章總數。
- 公開文章數。
- 隱藏文章數。
- 草稿文章數。
- 公開留言數。
- 隱藏留言數。
- 圖片引用與缺失數量。

使用者可在產生前確認資料是否符合預期。

### 7.3 網誌預覽頁

按下「產生網誌」後，介面切換至獨立預覽畫面。技術上建議使用前端路由或頁面狀態完成，避免為了換頁而將備份上傳至伺服器。

預覽頁包含：

- 生成後的網誌首頁。
- 文章列表與月份封存。
- 單篇文章閱讀功能。
- 上鎖文章閱讀確認功能。
- 桌面版與手機版預覽切換。
- 「返回設定」按鈕。
- 「下載網站 ZIP」按鈕。

預覽內容應在 sandboxed iframe 或等效的隔離區域內呈現，降低歷史文章 HTML 影響產生器介面的風險。

## 8. 輸入檔案需求

### 8.1 必要檔案

| 檔案格式 | 用途 |
|---|---|
| `*_blog.xml` | Blog 資料、文章、日期、公開狀態、留言與回覆 |
| `*-movable-type.txt` | 標籤、發布狀態、圖片網址及資料交叉驗證 |

介面不應把特定使用者名稱或匯出日期寫死。以下皆應視為有效名稱：

```text
wretch_2013-09-02_blog.xml
myblog_2013-08-30_blog.xml

wretch_2013-09-02-movable-type.txt
myblog_2013-08-30-movable-type.txt
```

### 8.2 選用檔案

後續版本可允許使用者選擇圖片資料夾或多個圖片檔：

```text
.jpg .jpeg .png .gif .bmp .webp
```

MVP 若未提供圖片，使用「歷史圖片已遺失」占位提示，不得阻止網站產生。

### 8.3 不需提供

MVP 不處理以下備份：

```text
profile_*.txt
guestbook_*.txt
digu_*.txt
join_*.txt
album/*
audio/*
video/*
```

## 9. 產生規則

### 9.1 公開狀態

| 原始值 | 顯示方式 |
|---|---|
| `isCloak=0` | 公開文章，直接顯示全文 |
| `isCloak=1` | 顯示「隱藏」標籤、標題右側鎖頭及閱讀確認遮罩 |
| `isCloak=2` | 顯示「草稿」標籤、標題右側鎖頭及閱讀確認遮罩 |

首頁與月份封存中的上鎖文章只顯示日期、標題、狀態及「點擊標題輸入密碼解鎖」，不顯示摘要、標籤與留言數。

### 9.2 留言

- 公開留言可以輸出。
- 上鎖文章的公開留言與正文一起展開。
- 原始備份標記為隱藏的留言預設不輸出。

### 9.3 HTML 安全處理

產生器必須清理歷史文章 HTML：

- 移除 `script`、`iframe`、`object`、`embed`、`applet`。
- 移除 `javascript:` URL。
- 移除 `onclick`、`onerror` 等事件屬性。
- 保留一般文字格式、表格、連結及安全圖片。

## 10. 輸出內容

產生結果應包含：

```text
index.html
blog/*.html
archive/*.html
page/*/index.html
css/*.css
js/*.js
assets/*
.nojekyll
build-info.json
```

MVP 提供兩種結果：

1. 瀏覽器內即時預覽。
2. 可下載的靜態網站 ZIP。

ZIP 可由使用者自行解壓縮後本機保存，或部署至 GitHub Pages、Cloudflare Pages、Netlify 等靜態網站空間。產品不代替使用者執行發布。

## 11. 建議技術架構

```text
GitHub Pages 靜態產生器
        │
        ▼
Browser File API
        │
        ▼
XML Parser + Movable Type Parser
        │
        ▼
資料比對、HTML 清理與標準化
        │
        ▼
前端模板產生文章與封存頁面
        │
        ├── 隔離預覽
        └── ZIP 匯出
```

建議採用：

- HTML、CSS、TypeScript 或 Vanilla JavaScript。
- 瀏覽器內建 `DOMParser` 解析 XML。
- 本機打包的 HTML sanitizer，不從 CDN 載入執行碼。
- 本機打包的 ZIP library。
- Web Worker 執行大型備份解析，避免頁面無回應。
- Content Security Policy 限制遠端連線與可執行內容。
- 不加入追蹤碼、廣告、遠端字型或使用者行為分析服務。

## 12. 可行性分析

### 12.1 整體判斷

整體可行性為「中高」。GitHub Pages 可以提供產生器介面，瀏覽器也能直接讀取使用者主動選擇的本機檔案，而不需上傳伺服器。

目前範例備份的兩個主要文字檔合計約 1.1 MB、包含 205 篇文章及 475 則留言，現代桌面與手機瀏覽器足以在本機處理。

### 12.2 可直接沿用的部分

- `isCloak` 公開狀態對應規則。
- XML 與 Movable Type 的資料合併邏輯。
- 日期、文章 slug 與留言關聯規則。
- HTML 清理允許清單。
- 既有橘色秋天版型、文章模板與 RWD。
- 閱讀確認遮罩的互動規則。
- 缺失圖片占位方式。

### 12.3 需要重新開發的部分

目前產生器以 Python、Jinja2 及本機檔案系統運作，不能直接在 GitHub Pages 的瀏覽器中執行。產品化需要：

1. 將 XML Parser、Movable Type Parser 與資料合併流程改寫為前端 JavaScript／TypeScript。
2. 將 Jinja2 模板改為瀏覽器可執行的模板或前端元件。
3. 建立瀏覽器內的虛擬輸出檔案結構。
4. 建立安全隔離的預覽機制。
5. 建立 ZIP 匯出功能。
6. 補上檔案格式錯誤、編碼問題、超大檔案及不完整備份的處理。

### 12.4 主要風險

| 風險 | 影響 | 建議處理 |
|---|---|---|
| 不同使用者的備份格式可能有差異 | 解析失敗或欄位缺漏 | 蒐集匿名格式樣本並建立 fixture 測試 |
| 歷史文章含不安全 HTML | 影響產生器或預覽頁 | 清理 HTML，並在 sandboxed iframe 顯示 |
| 大型備份造成頁面卡住 | 使用體驗不佳 | 使用 Web Worker、進度提示與檔案大小限制 |
| 使用者誤以為密碼代表加密 | 私密內容被公開 | 在設定、預覽與下載前重複顯示明確警語 |
| 遠端依賴或分析碼接觸內容 | 隱私風險 | 所有依賴本機打包，停用追蹤與遠端請求 |
| 手機瀏覽器記憶體有限 | 預覽或 ZIP 失敗 | MVP 優先支援桌面，手機提供檔案大小提示 |

## 13. 驗收標準

### 13.1 檔案與資料

- 使用者可以選取一個 `*_blog.xml` 與一個 `*-movable-type.txt`。
- 系統不發送包含備份內容的網路請求。
- 系統能顯示解析後的文章及留言統計。
- 缺少必要檔案、格式錯誤或檔案不匹配時，顯示可理解的錯誤訊息。
- 原始文章、留言與日期不得被自動改寫。

### 13.2 產生與預覽

- 按下「產生網誌」後能進入預覽畫面。
- 公開文章可直接閱讀。
- 隱藏與草稿文章只在密碼正確後於畫面展開。
- 錯誤密碼不展開文章。
- 重新載入預覽後恢復閱讀確認遮罩。
- 預覽頁可返回設定畫面。
- 使用者可下載包含完整靜態網站的 ZIP。

### 13.3 隱私

- 未經使用者操作，不上傳備份與產生成品。
- 不自動保存文章至伺服器或雲端。
- 不加入第三方分析、廣告或遠端執行碼。
- 下載前明確提示閱讀確認密碼不具加密效果。

## 14. 開發階段

### Phase 1：瀏覽器解析原型

- 建立產生器 `index.html`。
- 完成兩個必要檔案的選取與驗證。
- 將現有 Parser 與資料合併邏輯移植到前端。
- 顯示 Blog 與文章統計。

### Phase 2：網誌預覽

- 移植既有版型。
- 產生首頁、文章、月份封存與分頁。
- 加入閱讀確認密碼設定。
- 加入隔離預覽、錯誤提示與進度狀態。

### Phase 3：匯出與相容性

- 產生靜態網站 ZIP。
- 驗證 ZIP 內部連結及檔案完整性。
- 增加不同備份格式的測試樣本。
- 完成桌面及手機瀏覽器相容性測試。

### Phase 4：選用功能

- 載入本機歷史圖片。
- Theme 選擇與配色設定。
- Blog 名稱及描述調整。
- 匯出前選擇是否包含草稿或隱藏文章。
- 提供真正加密上鎖文章的進階模式。

## 15. Review 重點與待決策事項

1. **部署位置**：目前個人網誌已使用正式站根目錄的 `index.html`。產生器若也使用根目錄，兩者會衝突。建議將產生器建立成獨立 Repository／GitHub Pages 網站，或放在現有網站的 `/generator/` 子路徑。
2. **預覽形式**：建議採用同一個前端應用程式中的預覽畫面，網址可顯示為 `index.html#/preview`，避免換頁時必須暫存完整備份。
3. **MVP 是否包含 ZIP**：只有預覽仍無法保存產生結果；建議將 ZIP 下載列為 MVP。
4. **密碼規則**：可允許使用者自行設定，但必須持續標示為閱讀確認遮罩。
5. **輸出範圍**：建議在匯出前讓使用者選擇是否包含 `isCloak=1/2` 文章，避免誤將私人內容發布。
6. **手機支援**：手機可閱讀產生器，但大型備份解析及 ZIP 產生建議先以桌面瀏覽器為主要驗收平台。

## 16. 結論

本產品可以製作成完全由 GitHub Pages 提供的瀏覽器端工具，並在不將備份上傳至伺服器的前提下完成解析、預覽與 ZIP 匯出。

最大的開發工作是把現有 Python／Jinja2 產生流程移植到瀏覽器端。最大的產品風險是使用者把閱讀確認遮罩誤解為安全保護，因此所有與上鎖文章相關的畫面都必須明確揭露：公開靜態網站後，文章正文仍可能被讀取。
